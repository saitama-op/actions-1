name: my first workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Repo
              uses: actions/checkout@v6

            - name: Welcome message
              run: echo "My first github workflows"

            - name: List files
              run: ls
            
            - name: read files
              run: cat README.md